---====== Load spawner ======---
 
local spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/V2/Source.lua"))()
 
---====== Create entity ======---
 
local entity = spawner.Create({
    Entity = {
        Name = "A60",
        Asset = "https://github.com/eliazbp92-collab/Back1/raw/main/Place_131351567799504_Model_A60Client_1788136704.rbxm",
        HeightOffset = 0
    },
    Lights = {
        Flicker = {
            Enabled = false,
            Duration = 1
        },
        Shatter = true,
        Repair = false
    },
    Earthquake = {
        Enabled = false
    },
    CameraShake = {
        Enabled = true,
        Range = 200,
        Values = {1.5, 20, 0.1, 1} -- Magnitude, Roughness, FadeIn, FadeOut
    },
    Movement = {
        Speed = 350,
        Delay = 4,
        Reversed = false
    },
    Rebounding = {
        Enabled = false,
        Type = "Ambush", -- "Blitz"
        Min = 2,
        Max = 5,
        Delay = 1
    },
    Damage = {
        Enabled = true,
        Range = 80,
        Amount = 125
    },
    Crucifixion = {
        Enabled = true,
        Range = 40,
        Resist = false,
        Break = true
    },
    Death = {
        Type = "Guiding", -- "Curious"
        Hints = {"You died to Hardcore A-60...", "He will announce his presence with a loud scream.", "Hes fast and he can rebound, so stay hidden until hes gone!", "Im Sure Youll Make it!"},
        Cause = ""
    }
})
 
---====== Debug entity ======---
 
entity:SetCallback("OnSpawned", function()
    print("Entity has spawned")
	local entity = workspace["A60"]:FindFirstChildWhichIsA("BasePart")
	entity.Position = Vector3.new(0, 0, 5000)
	
	local function GetGitSound(GithubSnd, SoundName)
		local url = GithubSnd
		if not isfile(SoundName .. ".mp3") then
			writefile(SoundName .. ".mp3", game:HttpGet(url))
		end
		local sound = Instance.new("Sound")
		sound.SoundId = (getcustomasset or getsynasset)(SoundName .. ".mp3")
		return sound
	end
	local Ambience = GetGitSound("https://github.com/eliazbp92-collab/Back1/raw/main/65F10783-A0D6-418A-B79F-D37C9DEC91EC.mp3","A60hackey")
	Ambience.Parent = workspace["A60"].RushNew
	Ambience.Volume = 2.5
	Ambience.Looped = true
	Ambience.PlaybackSpeed = 1
	Ambience.RollOffMinDistance = 100
	Ambience.RollOffMaxDistance = 200
	Ambience:Play()
end)
 
entity:SetCallback("OnStartMoving", function()
    print("Entity has started moving")
	local entity = workspace["A60"]:FindFirstChildWhichIsA("BasePart")
	entity.Position = Vector3.new(0, 0, 5000)
	
	local function GetGitSound(GithubSnd, SoundName)
		local url = GithubSnd
		if not isfile(SoundName .. ".mp3") then
			writefile(SoundName .. ".mp3", game:HttpGet(url))
		end
		local sound = Instance.new("Sound")
		sound.SoundId = (getcustomasset or getsynasset)(SoundName .. ".mp3")
		return sound
	end
	local Ambience = GetGitSound("https://github.com/eliazbp92-collab/Back1/raw/main/doors-rooms-a-60-distant-sound-effect-made-with-Voicemod.mp3","A60hackey2")
	Ambience.Parent = workspace["A60"].RushNew
	Ambience.Volume = 1
	Ambience.Looped = true
	Ambience.PlaybackSpeed = 0.1
	Ambience.RollOffMinDistance = 100
	Ambience.RollOffMaxDistance = 200
	Ambience:Play()
end)
 
entity:SetCallback("OnEnterRoom", function(room, firstTime)
    if firstTime == true then
        print("Entity has entered room: ".. room.Name.. " for the first time")
    else
        print("Entity has entered room: ".. room.Name.. " again")
    end
end)
 
entity:SetCallback("OnLookAt", function(lineOfSight)
    if lineOfSight == true then
        print("Player is looking at entity")
    else
        print("Player view is obstructed by something")
    end
end)
 
entity:SetCallback("OnRebounding", function(startOfRebound)
    if startOfRebound == true then
        print("Entity has started rebounding")
    else
        print("Entity has finished rebounding")
    end
end)
 
entity:SetCallback("OnDespawning", function()
    print("Entity is despawning")
end)
 
entity:SetCallback("OnDespawned", function()
    print("Entity has despawned")
loadstring(game:HttpGet("https://raw.githubusercontent.com/eliazbp92-collab/Back1/refs/heads/main/Multi"))()
end)
 
entity:SetCallback("OnDamagePlayer", function(newHealth)
    if newHealth == 0 then
        print("Entity has killed the player")
    else
        print("Entity has damaged the player")
    end
end)
 
--[[
 
DEVELOPER NOTE:
By overwriting 'CrucifixionOverwrite' the default crucifixion callback will be replaced with your custom callback.
 
entity:SetCallback("CrucifixionOverwrite", function()
    print("Custom crucifixion callback")
end)
 
]]--
 
---====== Run entity ======---
 
entity:Run()
